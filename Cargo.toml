[package]
name = "nixos-opencv-win"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
opencv = { version = "0.77", default-features = false, features = ["imgproc", "imgcodecs"] }

